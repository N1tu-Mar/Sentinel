# scenarios sources
Generated by fixtures/_gen.py (edit there, re-run). No live captures (no Arga creds). Doc-derived:
- Seeds reuse Tier 1/2 shapes. IDs are illustrative: after Arga seeds from scenario_prompt, map by customer email, not by ID.
